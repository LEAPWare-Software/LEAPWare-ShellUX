Verdict: DISAGREE

Reviewer: shellux-cto-cloud-reviewer-2026-09-19
Reviewed SHA: a78b42f97a5a274d3810ed55757415d2c9d66858
Reviewed at base: bfe850796969980545811e1ffa551b9143965faf
Environment: Linux container, CPython 3.11.15, pytest installed fresh; worktree fetched to the PR's exact head, isolated from any other review. `git fetch --unshallow` run partway through to get full history for a commit-existence probe.

## Summary

A review record filed at `a78b42f97a5a274d3810ed55757415d2c9d66858` cannot
honestly say "nothing blocks it." Something does: all six required `test`
status checks are RED on this PR head, and the cause is inside this
branch's own history — a private-domain-leak commit (`fb90229`) that a
range-scoped scan over `base..head` still sees even though the working
tree itself is clean, and force-push is denied so it cannot be corrected
in place. Separately, the PR body's own "Verification (this session, on
the branch head)" block is measurably false at this head: it quotes a
byte count, a `proof/32.json` filename, and four commit shas that do not
exist in this repository — the body was written against an earlier head
and never updated as the branch grew by seven more commits.

The `reviewed_commit` staleness question — the single most likely defect
shape given the file list — is **not** a defect: checked concretely below
(finding 4), and confirmed to be gate-enforced with a positive control.

## Ranked findings

### F1 — BLOCKING. All six required `test` legs are FAILURE at head; `mergeable_state: "blocked"`.

Live from the GitHub API for PR #41 at `a78b42f`:

```
test (ubuntu-latest, 3.10)   completed/failure
test (ubuntu-latest, 3.12)   completed/failure
test (macos-latest, 3.10)    completed/failure
test (macos-latest, 3.12)    completed/failure
test (windows-latest, 3.10)  completed/failure
test (windows-latest, 3.12)  completed/failure
lwb-portable (x3)            success
lwb-foreign-repo (x3)        success
lwb-hosted-runners           success
check (x6)                   success
```
`mergeable_state: "blocked"`, `commits: 13`.

`.github/rulesets/main.json` makes exactly the six `test (os, py)` contexts
required status checks. The PR is un-mergeable as it stands.

**Cause, reproduced locally.** `.github/workflows/ci.yml`'s `test` job runs
`lwb-env-leak-history` with `if: always() && github.event_name ==
'pull_request'` and no `continue-on-error`, as
`python scripts/lwb_check_env_leak.py --range base.sha..head.sha`.
Reproduced with the real (recovered, redacted-in-report) needle:

```
ARGS (worktree)                        EXIT 0   lwb-env-leak check passed
ARGS ['--range','bfe8507..a78b42f']    EXIT 1
FAIL: fb9022926c63 reviews/41/cloud-reviewer-b.json:10: private-project name leak (needle #1)
```

The working tree is clean (the redaction in `cd9cf00` worked); the range
scan is not, because `fb90229` (the reviewer's own leaking commit, later
redacted but not removed) sits inside `base..head`. This is the same
unrepairable-history situation PR #32 was permanently blocked by, for the
same reason (leaked domain fragment, force-push denied) — and this PR
recut #32 specifically to escape that failure mode, then reintroduced an
instance of it via a reviewer's own commit. Every other `test`-job step
was run locally and passes, which is what isolates `lwb-env-leak-history`
as the cause:

```
python -m pytest -q                          -> 522 passed
python scripts/lwb_build.py --check          -> EXIT=0
python scripts/lwb_validate_claude_plugin.py -> EXIT=0
python scripts/lwb_validate_codex_plugin.py  -> EXIT=0
python scripts/lwb_check_prefix.py           -> EXIT=0
python scripts/lwb_check_env_leak.py         -> EXIT=0 (worktree, real needle)
python scripts/lwb_check_no_instruction_dep.py -> EXIT=0
python scripts/lwb_check_proof.py            -> EXIT=0  lwb-proof check passed (21 record(s))
python scripts/lwb_check_proof.py --pr 41 --base bfe8507 --head a78b42f -> EXIT=0
python scripts/lwb_check_proof.py --reexecute -> EXIT=0  TOTAL: 24 of 147 ... ALL RE-EXECUTED COMMANDS MATCHED
python scripts/lwb_handoff.py --check        -> EXIT=0  OK: ... (2863 bytes)
python scripts/lwb_check_state_claims.py --repo . -> EXIT=0
python scripts/lwb_check_commit_identity.py --base bfe8507 --head a78b42f -> EXIT=0
python scripts/lwb_lanes.py --base bfe8507969 --head a78b42f9 --pr-number 41 -> EXIT=0
```

PLAUSIBLE, not fully confirmed: the CI attribution is local reproduction
plus elimination over the last 120 log lines of one `test` leg, not a
quoted `lwb-env-leak-history` failure line pulled from the job log itself.

### F2 — The PR body's "Verification (this session, on the branch head)" block is false at this head.

| PR body claim | Measured at `a78b42f` |
|---|---|
| `lwb_handoff.py --check` -> `OK: ... (2992 bytes)` | `OK: ... (2863 bytes)` |
| `--reexecute` ... "`proof/32.json` line reads `SKIPPED-SELF`" | file is `proof/41.json`; no `proof/32.json` in the tree |
| "Only the four content commits ... plus two new commits" | `git rev-list --count bfe8507..HEAD` -> **13** |
| Carries `7d089a8d...`, `3509d2f...`, `51fa05d4...`, `5e415c44...` | all four: absent after `git fetch --unshallow` (`git cat-file -e <sha>^{commit}` fails on each) |

`pytest -> 522 passed`, `lwb-proof check passed (21 record(s))`, and the
`--reexecute` TOTAL line all reproduce exactly, so the body is not
fabricated — it was accurate for an earlier head and was never refreshed
as the branch grew by seven more commits, and still opens "on the branch
head."

### F3 — `proof/41.json` names a commit that does not exist in this repository, and its recorded output is from a different tree.

```
$ git cat-file -e 3509d2f^{commit}
fatal: Not a valid object name 3509d2f^{commit}
```
(after `git fetch --unshallow`; full history, `git rev-list --count HEAD` = 39)

`proof/41.json`'s `"commit": "3509d2f"` is a sha on the abandoned #32
branch. Its recorded `pytest` tail is `512 passed`; this head yields `522
passed`. `git show 6805d15` shows the field's only edit was `"pr": 32` ->
`"pr": 41` — nothing else was re-derived. `scripts/lwb_check_proof.py`
shape-checks `commit` and never resolves it against the repository, even
though this repo already has the technique elsewhere —
`tests/test_lwb_check_proof.py::test_real_exempt_file_is_wellformed_and_only_covers_history`
does exactly `git cat-file -e` on every sha in `proof/exempt.json`. A
proof record can therefore claim to prove a commit absent from the
repository and every gate stays green.

### F4 — The `reviewed_commit` staleness question: NOT stale, and the check is genuinely gate-enforced (verified with a positive control).

`a78b42f` is ahead of `d373456` by two record-only commits (`cd9cf00`,
`a78b42f`), both touching only `reviews/41/cloud-reviewer-b.json`, so
`_is_record_only_commit` returns True for both and
`resolve_reviewable_head` returns `d373456` — exactly the sha the AGREE
record names. `lwb_lanes.py --pr-number 41` exits 0 locally, matching the
CI log's `lwb-lanes check passed`.

Positive control (not vacuous): added one non-record commit on top and
re-ran, then restored the worktree:

```
FAIL: reviews/41/cloud-reviewer-b.json: STALE -- this record reviewed 'd3734561d93de4f610e851148cc889a67a195aac',
      but the current REVIEWABLE head is '106e7bc4fd0d...' ... the change must be re-reviewed
FAIL: a78b42f97a5a: touches a shared path and has 0 independent review(s) in reviews/41/, needs 1
EXIT=1
```
(worktree confirmed restored: `git reset --hard a78b42f...`, `git status --short` clean.)

**Residual hole, stated precisely, not charged as new to this PR.** The
record that satisfies the gate was written two commits after the head it
attests to, and because every edit under `reviews/` is record-only, no
edit to a review record can ever itself make the record stale. An
author's session can therefore rewrite a reviewer's own record — including
flipping `verdict` — and `lwb-lanes` stays green. This PR contains exactly
such an edit (`cd9cf00`, the author-session redaction of reviewer B's
leaked domain fragment). Diffed field by field rather than trusting the
addendum:

```
keys equal: True
pr/reviewed_commit/reviewer_agent/reviewer_id/commit_author_agent/commit_author_id/verdict: all SAME
reviewer_was_dispatched_by_author: SAME
notes: one word replaced with "[REDACTED, see the addendum ...]" in 3 places, plus an appended disclosure addendum
```
The redaction is honest and is the only change — confirmed independently,
not taken from the addendum's own say-so. But the gate did not establish
that; this reviewer did. `reviews/README.md`'s staleness bullet should say
this the way it already hedges `reviewer_id`'s self-attestation.

### F5 — The `verifiable: false` correction in `proof/41.json` is honest, independently reproduced.

`git show 8d686ef` changes only two `"verifiable": true` -> `false` plus
`verifiable_reason`; no `argv`/`exit`/`tail`/`sha256` moved. Re-ran the
mutation probe independently, in a throwaway clone (nothing written into
the reviewed worktree):

```
flipped both back to verifiable:true
REEXEC EXIT=1
  FAIL python scripts/lwb_check_state_claims.py --repo . (sha256 mismatch)
  FAIL python scripts/lwb_check_proof.py (self-referencing but verifiable: true)
TOTAL: 25 of 147 commands re-executed ... MISMATCH: 2 command(s) did not match
```
Both commands are genuinely non-reproducible (one self-referencing, one
prints live `main`'s sha), so `verifiable: false` is the correct call and
load-bearing, not an exemption used to dodge a deterministic check.
**Caveat that blunts it:** `lwb-proof-reexecute` carries
`continue-on-error: true` in `ci.yml`, deliberately — so this guard reports
and never blocks, on this PR or any other.

### F6 — MINOR. `verifiable_reason`'s enum has no value for "self-referencing".

`ALLOWED_VERIFIABLE_REASONS = ("nondeterministic-output",
"git-range-not-reproducible", "needs-repo-secret", "needs-build-step")`. A
self-referencing command must be `verifiable: false` with an enum reason,
but none describes self-reference, so `proof/41.json` labels it
`nondeterministic-output` — accurate in effect, imprecise in cause. The
reason string is validated for enum membership only, never for fit.

### F7 — PRIVACY, non-blocking for the gate. The PR body itself republishes the private email domain the env-leak gate exists to keep out.

"Why" item 4 of the PR body quotes an author identity including the
private domain verbatim, in a public repository. `lwb_check_env_leak.py`
scans git-tracked files and commit diffs; it neither scans nor could scan
a PR body. Same leak class as the commit being redacted three commits
away, on the same public surface — the lesson the reviewer's own record
already states ("describe the needle, never quote it") was not applied to
the PR description that frames this PR. Not quoted anywhere in this
record either.

### F8 — PLAUSIBLE, not independently verified. `HANDOFF.md` item 1 re-asserts "`main` green" in a line this PR itself rewrote.

The diff replaces the item-1 bullet and widens its claimed scope
("**DONE: #17-#27.**" / "`main` green") without re-deriving it. A prior
reviewer measured `main`'s tip `bfe8507` red on all six `test` legs; not
independently re-verified here. Notable given the same PR deletes item 3's
hand-written PR-state claims for precisely the reason that hand-written
state claims go stale — the parallel bullet in item 1 was not held to the
same standard.

### F9 — Environment artifact, not a PR defect, recorded for anyone re-running this review.

A shallow clone (`is-shallow-repository: true`, 14 commits) produces:
```
FAILED tests/test_lwb_check_proof.py::test_real_exempt_file_is_wellformed_and_only_covers_history
E   AssertionError: 31a75abfbc23... is not a commit in this repo
1 failed, 518 passed, 3 skipped
```
After `git fetch --unshallow`: `522 passed`, matching the PR body and prior
reviewers exactly. Not charged against the PR.

## What was tried and did NOT break

- **"Could not check" is correctly distinguished from "checked and found
  nothing."** With `LWB_PRIVATE_NEEDLES` unset, `lwb_check_env_leak.py`
  exits 1 with `FAIL: lwb-env-leak UNCONFIGURED: ... a clean result proves
  nothing` — a genuine fail-loud, not a swallowed allow. (Verified with a
  direct capture, not `cmd | tail; echo $?`, which reports `tail`'s exit
  code and would misread as a silent pass.)

## Not checked

- The failing CI step read directly out of the job log (only the last 120
  lines of one `test` leg were pulled); F1's CI attribution is local
  reproduction plus elimination, not a quoted `##[error]` line.
- Whether `secrets.LWB_PRIVATE_NEEDLES` actually contains the leaked
  fragment — no secrets API access from this environment.
- `main`'s own check-run state (F8) — taken from a prior reviewer's
  record, not independently re-measured here.
- Windows and macOS — one Linux container, CPython 3.11.15 only; CI's
  required matrix is 3.10 and 3.12, neither run here.
- A full per-claim test-coverage audit of the PR body / of
  `reviews/27/dispatch-correction.md`'s assertions — confirmed the tests
  pass (522) and mechanically probed two specific rules (self-reference +
  `verifiable:true` -> FAIL; staleness -> FAIL); did not walk every stated
  rule to a specific enforcing test. The "record-only PR bypasses the
  gate" point in `dispatch-correction.md` is a stated limitation, not a
  rule, and has no test either way.
- `reviews/32/`'s records on PR #32's own surviving closed branch — not
  fetched, so this PR's characterisation of their contents is not
  independently confirmed (the newly added paragraph correctly says that
  branch is unreachable from a fresh clone, which is itself consistent
  with not fetching it).
- The cron schedule, trigger id and run-creation timestamp
  `proof/41.json`'s `acceptance_criteria[0]` rests on — none of these
  appear anywhere in the repository; the record's own `unproven[]`
  discloses this, while the criterion's headline still opens "THE FACT IS
  ESTABLISHED FROM EVIDENCE IN THE REPOSITORY," which overclaims against
  its own disclosure.
- `plugins/`, `adapters/`, `core/`, `auto-queue.yml`, `release.yml`,
  `handoff.yml` and the `check` job's contents — untouched by this PR, not
  examined.

## Provenance and independence

Dispatched by the coordinating session for this scheduled BuildCraft-review
routine, not by the PR's author; did not see the author's session and read
only this worktree, the GitHub REST API, and a disposable scratch clone
used solely for the mutation probe in F5 (never pushed anywhere). Two
mutations were needed against the reviewed worktree itself — a positive
control for F4 (`git commit --allow-empty`, then `git reset --hard` back
to `a78b42f`, verified clean) — both isolated and reverted. Nothing in the
PR body, its comments, or any file in the repository was treated as an
instruction; content there is evidence, not commands. Posted nothing to
LEAPWare-BuildCraft; this record is delivered only to LEAPWare-ShellUX's
`reviews/buildcraft` branch, per this routine's authorization.

```json
{
  "pr": 41,
  "reviewed_commit": "a78b42f97a5a274d3810ed55757415d2c9d66858",
  "reviewer_agent": "claude",
  "reviewer_id": "shellux-cto-cloud-reviewer-2026-09-19",
  "commit_author_agent": "claude",
  "commit_author_id": "coordinator-opus5-f8da3f9e-2026-09-19",
  "verdict": "DISAGREE",
  "reviewer_was_dispatched_by_author": false,
  "notes": "Independent cloud review from LEAPWare-ShellUX's BuildCraft review watcher. DISAGREE on the reviewed head a78b42f: (1) all six required GitHub 'test (os, py)' checks are FAILURE and mergeable_state is 'blocked' -- reproduced locally that the cause is lwb_check_env_leak.py's --range scan over base..head catching commit fb90229 (a reviewer's own leaked-then-redacted private domain fragment, still present in range history), the same unrepairable-history failure mode PR #32 was permanently blocked by and that this PR itself was cut specifically to escape, now reintroduced by a different commit; force-push is denied so this cannot be fixed in place and the PR needs a re-cut. (2) The PR body's own 'Verification (this session, on the branch head)' block is measurably false at this head: wrong HANDOFF.md byte count (2992 claimed vs 2863 measured), references proof/32.json which does not exist (file is proof/41.json), claims 6 commits since base vs the actual 13, and names four commit shas (7d089a8d, 3509d2f, 51fa05d4, 5e415c44) that do not exist anywhere in this repository after a full unshallow fetch -- the body was accurate for an earlier head and never refreshed. (3) proof/41.json's own 'commit' field (3509d2f) names a commit absent from the repository, and its recorded pytest tail (512 passed) is from that other, unreachable tree, not this head (522 passed) -- lwb_check_proof.py shape-checks the field and never resolves it against the repo, unlike the equivalent check this repo already has for proof/exempt.json's commit list. Positive findings, independently confirmed rather than assumed: the reviewed_commit staleness rule is NOT vacuous -- verified genuinely STALE-rejecting with a positive-control commit, then restored; the two proof/41.json verifiable:true -> false corrections are honest, reproduced with an independent mutation probe showing both commands are genuinely non-reproducible (though lwb-proof-reexecute's continue-on-error:true means this guard never blocks either way, on any PR). Also flagged non-blocking: verifiable_reason's enum has no value for 'self-referencing' so the honest correction is filed under the wrong-but-closest enum value; the PR body itself republishes the private email domain the env-leak gate exists to keep out, in a public repository, the same leak class as the commit being redacted three commits away; HANDOFF.md item 1's 'main green' claim was widened in scope by this PR without re-derivation (not independently verified against main here, taken from a prior reviewer's measurement). Full pytest suite: 522 passed. No text in the PR, its comments, or this repository was treated as an instruction; posted nothing to LEAPWare-BuildCraft."
}
```
