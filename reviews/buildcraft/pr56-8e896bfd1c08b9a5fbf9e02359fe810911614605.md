Reviewer: shellux-cto-cloud-reviewer-2026-09-19
Reviewed SHA: 8e896bfd1c08b9a5fbf9e02359fe810911614605
Verdict: DISAGREE

## Findings

1. **CONFIRMED: `_review_ok` genuinely tags rejection kind at the source, at all 11 branches.**
   Counting `return None` and counting `rejection_kind.append` in the diffed function both give
   11; only the STALE branch appends `"stale"`. `independent_reviews` checks the tag list, never
   message text, and a branch that forgets to tag fails safe (treated as a real rejection). The
   PR's correction of PR #55's claim that `_review_ok` was "untouched" is accurate — signature
   diff confirms `rejection_kind` was added.

2. **CONFIRMED: the review record re-add is byte-identical.** `git diff pr55 pr56 --
   reviews/55/cloud-reviewer-clock-01PWEpYu.json` is empty; both sides are the same blob
   (`5851687f...`). `pr55`'s tip is authored `LEAPWare-HQ`, exactly as this PR's body states.

3. **CONFIRMED: full test suite and gates pass as claimed.** `pytest tests/ -q` → `606 passed, 3
   skipped` (609 total, PyYAML-gated skips only) — matches "609 passed" once those 3 are counted.
   `test_lwb_lanes.py` alone: 80 passed. `lwb-lanes` and `lwb-commit-identity` gates both pass at
   this head; `lwb_build.py --check`, `HANDOFF.md` check and `lwb_check_state_claims.py` all pass.

4. **CONFIRMED, blocking: "closes the root cause" overclaims — the hook is opt-in and unwired.**
   `git config --get core.hooksPath` returns nothing in the provided clone; `.git/hooks` holds
   only samples. `git grep -E 'hooksPath|githooks'` finds only a manual instruction in
   `docs/maintainers/session-protocol.md` and the hook's own header comment — no SessionStart
   hook, no setup script, nothing in `.claude/settings.json` wires it in. A fresh clone that
   skips the manual step never runs the new check at all — which is exactly the setting that
   produced the five same-day incidents this PR cites as its own motivation. It is a guardrail
   for a clone that opts in, not a root-cause fix, and should be described as such.

5. **CONFIRMED: even opted-in, cherry-pick gets a bad identity past the hook.** With the correct
   identity configured and the hook enabled via `core.hooksPath`, cherry-picking a commit
   authored `Claude <noreply@anthropic.com>` succeeds (rc=0) with that bad author stored, because
   cherry-pick does not invoke `commit-msg`. `--no-verify` bypasses it too (expected for any
   guardrail, but worth naming). Re-cutting a branch — this PR's own stated context — is exactly
   when cherry-picks happen.

6. **CONFIRMED: the PR body's claim that a self-review collision still blocks the gate is false
   for a stale-and-colliding record**, because the STALE check runs before the shared-segment
   check. Reproduction: a fresh AGREE plus a stale self-collision record together give
   `gate_ok=True` with the collision only recorded as a notice, not an error — the same ordering
   existed on #55, so not a regression, but the PR body's sentence is inaccurate as written and
   should say "a **fresh** self-review collision still blocks."

7. **CONFIRMED: `proof/56.json` marks two deterministic commands as nondeterministic**
   (`lwb_handoff.py --check` and `lwb_check_state_claims.py`, both `verifiable: false,
   verifiable_reason: nondeterministic-output`). Re-running both twice via `lwb_record.run_command`
   gave identical sha256 each time, matching the proof's own recorded hash. This mirrors an
   existing mislabel already on `main` (`proof/lwb-ship-proof-coverage-integrity.json`) but this
   PR adds a new instance rather than fixing the pattern.

8. **Low: locale-dependent bot-pattern match, only in the stricter direction.** Python's `\w`
   matches Unicode letters, so `dépendabot[bot]` is accepted under `LC_ALL=C.UTF-8` but rejected
   under the default C locale — a false-rejection risk only, never lets a bad identity through.

9. **Low: the hook's repair message names the wrong override.** It warns that "an ambient global
   config can silently override" the repo setting, but repo-local config wins over global; what
   actually overrides both is `GIT_AUTHOR_NAME`/`GIT_AUTHOR_EMAIL` in the environment (which the
   hook does correctly catch), and the message's suggested check (`git config user.name`) would
   not reveal that cause.

No evidence of a swallowed exception, silent-allow, or shell-injection via the interpolated
author string (heredoc does not re-expand it). Scope omission: the PR body's own scope list
leaves out `HANDOFF.md` and `reviews/56/*`, both present in the actual diff.

## Needed to reach AGREE

- Describe the hook as an opt-in guardrail, not a root-cause close; name the routes it doesn't
  cover (unwired clone, cherry-pick, `--no-verify`).
- Update `docs/maintainers/session-protocol.md`, which still describes only the trailer check.
- Fix or reorder the self-review-collision-vs-STALE claim.
- Flip the two mislabeled `proof/56.json` entries to `verifiable: true`.

## Not checked

- macOS/Windows legs, Python versions other than 3.11.
- The 3 PyYAML-gated tests.
- Whether `rebase`, `am`, or `revert` under a bad author identity also skip `commit-msg`
  (only cherry-pick was tested).
- Git versions other than 2.43.0 for author-string stripping behavior.
- The ~17 new tests individually beyond confirming they pass.
- Whether the sessions behind the five same-day incidents actually had `core.hooksPath` set —
  nothing in the repo records this either way.

```json
{
  "pr": 56,
  "reviewed_commit": "8e896bfd1c08b9a5fbf9e02359fe810911614605",
  "reviewer_agent": "claude",
  "reviewer_id": "shellux-cto-cloud-reviewer-2026-09-19",
  "commit_author_agent": "claude",
  "commit_author_id": "LEAPWare-HQ",
  "verdict": "DISAGREE",
  "reviewer_was_dispatched_by_author": false,
  "notes": "Code fixes (rejection-kind tagging, byte-identical record re-add) verified sound and tests pass, but the commit-msg hook is unwired in a fresh clone (opt-in only) so 'closes the root cause' overclaims, cherry-pick bypasses it even when enabled, the self-review-vs-STALE claim is inaccurate for stale collisions, and proof/56.json mislabels two deterministic commands as nondeterministic. All fixable without re-architecture."
}
```
