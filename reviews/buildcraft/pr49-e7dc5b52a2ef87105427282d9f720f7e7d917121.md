Verdict: DISAGREE

Reviewer: shellux-cto-cloud-reviewer-2026-09-19 (external cloud peer review, LEAPWare-ShellUX watching LEAPWare-BuildCraft, read-only)
PR: LEAPWare-Software/LEAPWare-BuildCraft#49 — "Ship lwb_proof_coverage and lwb_proof_integrity as rules (1.1)"
Reviewed commit: e7dc5b52a2ef87105427282d9f720f7e7d917121
Model: opus (dispatched per lw-verifier role, BUDGET 60k — this PR ships new PreToolUse-hook gate rules, proof-record self-certification detection)

## Summary

DISAGREE, on the same permanent-block grounds as this routine's prior DISAGREE on #47: this head cannot ever pass this repo's own gates, so it is not mergeable code regardless of the soundness of its content. Two independent blockers were each reproduced directly rather than taken on the PR's word. First, `lwb_check_commit_identity.py --base origin/main --head HEAD` fails on commit `2a329d7`, authored `Claude <noreply@anthropic.com>` — a prior independent reviewer's own record commit, sitting inside the PR's commit range, not this repo's allow-listed identity. Second, `lwb_lanes.py --base origin/main --head HEAD --pr-number 49` fails because `reviews/49/cloud-reviewer-b.json` carries `"verdict": "DISAGREE"`; this repo's own `_review_ok()` treats any non-AGREE record as a hard, unconditional error that no later AGREE record can outvote. Deleting another reviewer's record to clear it would be evidence-tampering, so — per this routine's own reasoning on #40→#45, #43→#46 and #44→#47 — the branch needs a re-cut, not a fix-in-place. That re-cut already exists and is open as #50, reviewed separately by this routine in the same run. The underlying bug that DISAGREE record reports (proof-coverage's `known` set built only from proof-record *filenames*, missing a record that declares its PR number in *content* instead) is real: reproduced by running the fix's own regression test against a copy of the pre-fix code at `bcbf0e1`, where it fails, and confirming it passes with the fix (`193a389`) already applied on this same branch at HEAD. So the content itself is not the problem; the branch's own history is permanently unclearable.

## Findings, most severe first

### CONFIRMED F1 (blocking, permanent). `lwb-commit-identity` fails on this branch and cannot be cleared without rewriting another actor's commit

```
$ git log --format='%h %an <%ae> | %cn <%ce> | %s' origin/main..HEAD
e7dc5b5 LEAPWare <leapware@outlook.com> | ... | reviews/49: independent cloud review — AGREE on content, PR unmergeable
193a389 LEAPWare <leapware@outlook.com> | ... | fix(1.1): lwb_proof_coverage misses records that declare their PR in content
2a329d7 Claude <noreply@anthropic.com> | Claude <noreply@anthropic.com> | reviews/49: independent cloud review B — DISAGREE
bcbf0e1 / 72bd30e / 44d7a76 / 560b68a  LEAPWare <leapware@outlook.com> | ...

$ python3 scripts/lwb_check_commit_identity.py --base origin/main --head HEAD
FAIL: commit author not LEAPWare or an allow-listed bot: Claude <noreply@anthropic.com>
EXIT=1
```
The script scans every commit's author across `base..head`; `2a329d7` (another reviewer's own record commit) is permanently inside that range once it exists on the branch. No commit added on top removes it from the range. Matches the exact shape that already forced #40→#45, #43→#46 and #44→#47.

### CONFIRMED F2 (blocking, but not itself permanent in code — permanent only by this repo's own tamper rule). `lwb-lanes` fails on a DISAGREE record; deleting it clears the gate mechanically, but doing so is evidence-tampering and is correctly not done

```
$ python3 scripts/lwb_lanes.py --base e5f29bfaccec90199f983b778c1d7689b8c24d33 --head HEAD --pr-number 49
FAIL: reviews/49/cloud-reviewer-b.json: verdict is 'DISAGREE', want 'AGREE'   (repeats once per commit)
EXIT=1
```
`_review_ok()` appends any non-AGREE record's verdict as a hard error and ORs it into the build's errors; the "needs 1 independent review" count is already satisfied by a separate AGREE record (the clock reviewer's), but the one DISAGREE record is never outvoted. In an isolated scratch copy with only that one file removed, the same check exits 0 — confirming the block is a deliberate rule (no rewriting or deleting another reviewer's record) rather than an unfixable defect in the check itself. That rule is the correct one to keep; it is why this PR must stay closed via re-cut (#50) rather than patched.

### CONFIRMED F3 (informational, not a defect). The content-declared-PR bug the blocking DISAGREE record reports is real at the pre-fix commit, and is fixed on this same branch by the time of HEAD

Reproduced by extracting the pre-fix code (`bcbf0e1`) into a scratch tree and running the fix commit's own regression test (which constructs a proof record named after its branch, declaring `{"pr": 77}` in content, for a landed commit subject `"... (#77)"`) against it:
```
FAILED tests/adapters/test_claude_repo_facts_landed_unproven.py::test_a_record_named_after_its_branch_but_declaring_the_pr_is_silent
E  AssertionError: assert ('af8193489220 (#77)',) == ()
1 failed, 17 passed in 1.21s
```
The same test passes as part of the full suite at HEAD (see below), after fix commit `193a389` adds `_collect_proven_pr_numbers`. This PR's own content is not what should block it; its commit history is.

### CONFIRMED F4 (non-blocking, environment artifact of this review, not the PR). One suite failure traced to a shallow clone, not to the PR's code

```
$ python3 -m pytest tests/ -q -p no:cacheprovider
FAILED tests/test_lwb_check_proof.py::test_real_exempt_file_is_wellformed_and_only_covers_history
1 failed, 591 passed, 1 warning in 29.60s
E  AssertionError: 31a75abfbc23770b157b804322088130ac62aa28 is not a commit in this repo
$ git rev-parse --is-shallow-repository   -> true
```
31a75ab is a real upstream commit (GitHub shows it as "Bootstrap LEAPWare BuildCraft (lwb) scaffold (#1)"), absent only because this review's checkout is a shallow clone. The PR body's claimed "592 passed" was not independently confirmed on a full-history clone; the other 591 tests pass.

## Claims that held

- Both new rules are wired in: `core/lwb_core/rules/lwb_proof_coverage.py` and `lwb_proof_integrity.py` are imported and registered, `core/policy/default.json` sets both to `{"mode": "warn"}`.
- The rule tests are non-vacuous: `pytest -k "proof_coverage or proof_integrity"` gives 38 passed; forcing `gaps = ()` and the self-certification branch to `False` in a scratch copy makes 10 of those 38 fail, including the exact findings-production tests for both rules and both policy-mode-honoured tests.
- The DISAGREE record (`reviews/49/cloud-reviewer-b.json`, commit `2a329d7`) is genuinely authored `Claude <noreply@anthropic.com>`, and the later clock-review AGREE commit (`e7dc5b5`) is genuinely authored `LEAPWare <leapware@outlook.com>` — both confirmed by direct `git log` inspection, not taken from either record's own self-description.
- The checkout was left unmodified; all probes ran in separate scratch copies.

## Not checked

- Whether `lwb-commit-identity` and `lwb-lanes` are configured as *required* GitHub branch-protection checks, or whether an admin merge could bypass either.
- A full-history (non-shallow) run confirming the PR body's exact "592 passed" figure.
- An actual commit that deletes the DISAGREE record, to see whether such a commit would itself be accepted by lane discipline (not attempted — would be evidence-tampering against a live PR and was correctly avoided).
- The DISAGREE record's second, non-blocking finding (an `Optional[bool]` collapse in `collect_matched_proof_facts`) and the clock record's ~81 ms/hook-event performance finding.
- Windows/macOS legs, the vendored hook end to end, `lwb_build.py --check`, and the other gate scripts beyond `lwb_check_commit_identity.py` and `lwb_lanes.py`.
- A full fresh-content review of the rules and docs themselves — PR #50 (the re-cut carrying the same content plus this fix) receives that depth separately in this same review run.

```json
{
  "pr": 49,
  "reviewed_commit": "e7dc5b52a2ef87105427282d9f720f7e7d917121",
  "reviewer_agent": "claude",
  "reviewer_id": "shellux-cto-cloud-reviewer-2026-09-19",
  "commit_author_agent": "claude",
  "commit_author_id": "LEAPWare <leapware@outlook.com> (LWB-Agent: claude trailer, Claude-Session: session_012CYfXUsJMrpqknbwzPFLGC for the substantive content; GitHub PR author LEAPWare-HQ — self-attested, not independently verifiable)",
  "verdict": "DISAGREE",
  "reviewer_was_dispatched_by_author": false,
  "notes": "Independently reproduced both permanent blockers: lwb_check_commit_identity fails on 2a329d7 (authored Claude <noreply@anthropic.com>), and lwb_lanes fails on reviews/49/cloud-reviewer-b.json's DISAGREE verdict, which no later AGREE record can outvote and which may not be deleted without evidence-tampering. The underlying bug that DISAGREE record reports (proof-coverage missing a content-declared PR number) is real at the pre-fix commit bcbf0e1 and is already fixed on this same branch by commit 193a389 -- so the content is sound but the branch's own commit history can never pass this repo's gates. Matches the exact re-cut pattern already applied to #40, #43 and #44; the re-cut, #50, is open and reviewed separately in this run."
}
```
