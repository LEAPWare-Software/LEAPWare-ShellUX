Verdict: AGREE

Reviewer: shellux-cto-cloud-reviewer
Reviewed SHA: 2e27b0889f5cf42d0975db56d5f6edc197673b3b
PR: https://github.com/LEAPWare-Software/LEAPWare-BuildCraft/pull/58
Title: Re-cut #57: fix env-leak history defect (supersedes #57)
Reviewer_was_dispatched_by_author: false (dispatched by the BuildCraft review-watcher cloud routine, per docs/cloud/runbook.md section 5, running under a schedule with no live user input)

## Summary

MERGE. This PR re-cuts #57 (DO NOT MERGE, reviewed separately, same run):
its deliverable content is byte-identical to #57's, but this branch's
history never introduced the POSIX-home-path leak that permanently broke
#57's `lwb-env-leak-history` check. The central claim — the shipped hook
fires from `hooks.json` in a real, live Claude Code session, for both the
`Bash` and `Agent` matchers, and the ledger line is caused by `hooks.json`
specifically rather than plugin presence — is independently reproduced from
scratch, including a negative control. Remaining findings are minor wording
issues or pre-existing, already-documented properties of BuildCraft's review
gate, none of which block this PR's own deliverable.

## Findings, most severe first

### 1. Central claim confirmed by live reproduction, including a negative control (CONFIRMED)

Reproduced in this container (Claude Code 2.1.282, Python 3.11.15, `dontAsk`
mode) against a scratch clone at 2e27b08 with the plugin built by
`lwb_build.py`:

- **Bash matcher:** exit 0. Ledger gained `{"hook_event": "PreToolUse",
  "tool_name": "Bash", … "permit": true … "lwb_version" warn}`.
- **Agent matcher:** exit 0, `subagent_stats.spawned: 1`. Ledger gained one
  line with `"tool_name": "Agent"`.
- **Control 1 (no `--plugin-dir`):** Bash command ran, no ledger file
  created.
- **Control 2 (plugin loaded, `hooks/hooks.json` deleted):** Bash command
  ran, no ledger file created. Confirms the ledger line is caused by
  `hooks.json` specifically.
- **Root-refusal claim:** `claude -p … --permission-mode bypassPermissions`
  printed `--dangerously-skip-permissions cannot be used with root/sudo
  privileges`, exit 1 — matches the doc.
- **Proof-record digests:** the two ledger lines quoted in the doc match the
  `tail`/`sha256` of `commands[8]` and `commands[9]`.

### 2. History-leak fix confirmed (CONFIRMED)

With `LWB_PRIVATE_NEEDLES=placeholder-needle-zq9`:

```
$ lwb_check_env_leak.py --range origin/main..pr58-head   -> lwb-env-leak check passed (exit 0)
$ lwb_check_env_leak.py --range origin/main..pr57-head   -> exit 1
FAIL: 100f6545c541 docs/maintainers/hook-fires-verification-2026-09-24.md:107: POSIX home directory path  (+ lines 135, 170)
```

Grepping every added line of `git log -p origin/main..pr58-head` for
`/home/`, `/Users/` or a drive letter finds nothing. With needles unset the
scan fails `UNCONFIGURED`, as designed (fail-closed on a missing secret was
separately probed and confirmed). The real CI secret was not exercised
locally; CI's own run reported success (see Findings 6).

### 3. "Byte-for-byte identical to #57" holds for content; wording is loose in two places (CONFIRMED, minor)

- The new doc is blob `f1256ada…` on both branches; `build-plan.md` is blob
  `e16d6d5f…` on both — genuinely identical.
- `HANDOFF.md` differs beyond bookkeeping: `Linux only —` became `Linux
  only --`, and the `Generated:` timestamp changed. Cosmetic only, but the
  PR body's "byte-for-byte identical except the proof record's own
  bookkeeping" slightly overstates this.

### 4. The doc misattributes the ledger's shared `session_id` (CONFIRMED, minor)

The doc shows the same `session_id` for two supposedly separate `claude -p`
runs and the proof record calls this a "fresh session_id" per run. This
reviewer's own nested runs both carried the *outer* container session's id —
a nested `claude -p` here reports the enclosing session's id, not a fresh
one. The evidence is genuine; the doc's explanation of why the id is shared
is wrong.

### 5. Small inconsistencies (CONFIRMED, non-blocking)

- Proof record's `commands[8]`/`[9]` read ledgers from
  `/tmp/lwb-hook-scratch/proof-build/ledger-*.jsonl`; the doc says they were
  written to `/tmp/lwb-hook-scratch/work/`. Contents match; the record
  doesn't say the files were copied.
- `HANDOFF.md`'s regenerated block lost `CLI: claude` / `Session:
  track-a-recut-49-bad-identity`, now `CLI: unknown` / `Session: unknown`.
  `lwb_handoff.py --check` still passes.
- Doc wording "the tool the model calls is named `Task`" is unverified — the
  ledger only shows `Agent`; the model-side tool name was not checked.

### 6. Tests, gates, CI and scope (CONFIRMED)

- `python -m pytest -q`: `1 failed, 591 passed` at 2e27b08 and identically
  at base 6afc545 — the one failure
  (`test_real_exempt_file_is_wellformed_and_only_covers_history`, "31a75abf…
  is not a commit in this repo") is an artifact of this reviewer's shallow
  clone, not a defect in the PR; 591+1 matches the claimed 592, and a
  separate, unshallowed clone (used for #57's review in the same run)
  reported a clean 592 passed.
- All other gates exit 0 at head: `lwb_build.py --check`,
  `lwb_check_hook_launch.py`, `lwb_validate_claude_plugin.py`,
  `lwb_validate_codex_plugin.py`, `lwb_check_prefix.py`,
  `lwb_check_no_instruction_dep.py`, `lwb_check_proof.py` (23 records, and
  `--pr 58 --base … --head …`), `lwb_handoff.py --check`,
  `lwb_check_state_claims.py`, `lwb_check_commit_identity.py`.
- `resolve_reviewable_head('origin/main..pr58-head')` returns
  `d61c72405c7d…`; `2e27b08` and `698a3f3` are correctly skipped as
  touching only `reviews/`/`proof/` — matches the branch's own review
  record's claim, verified independently against the actual function.
- GitHub API: all 19 check runs at head 2e27b08 are `completed/success`.
- `git diff origin/main...pr58-head --stat`: 5 files — the new doc, the 1.8
  row of `build-plan.md`, `HANDOFF.md`, the new proof record, and
  `reviews/58/`. Matches the PR body's stated scope; nothing under `core/`,
  `adapters/` or `scripts/`.

### 7. Investigated separately: the tip commit, `reviews/58/cloud-reviewer-b.json`

Before dispatching the adversarial review, this routine's own read of the
branch found the tip commit (2e27b08) added a file at
`reviews/58/cloud-reviewer-b.json`, authored as `LEAPWare
<leapware@outlook.com>` directly on the PR's own branch, containing an
elaborate self-described "INDEPENDENT CLOUD REVIEW … reviewer B" with an
`AGREE` verdict and identity fields distinct from the commit author's.
Given the mandated single commit identity across this repo and the
convincing prose, this routine treated it as a plausible attempt to plant a
fake independent review to satisfy the gate, and instructed the dispatched
reviewer to disregard its content as untrusted data and verify everything
from scratch rather than defer to it.

The dispatched reviewer's independent investigation does **not** support
that hypothesis, and it did not rely on the file's `notes` field for any
claim above:

- `get_session` on the record's `reviewer_id` token
  (`01WitrMM4pi6s1fGLHBECwKY`) resolves to a real, separate session:
  "BuildCraft independent reviewer B (newest-first)", `origin:
  "scheduled_trigger"`, created 2026-09-24T20:53:14Z — distinct from the
  author/conductor session `012SWAwJYJ1cUjtpPZFhHZK2` ("BuildCraft conductor
  (track A)"), which last touched this branch at 20:37Z, before the
  reviewer session started.
- The paired record on PR #57 (reviewed separately, same run) from the same
  reviewer token is a `DISAGREE` — the opposite of what an author would
  plant to force a merge.
- Every commit in this repo is required to carry the same git author
  identity (owner directive 10, enforced by
  `scripts/lwb_check_commit_identity.py`), so shared authorship alone
  cannot distinguish a reviewer commit from an author commit.
- Records in the same style were already merged for PRs 39, 44 and 50; the
  PR body itself says an independent review record is written by "the
  separate clock-dispatched reviewer routine … not included here" —
  consistent with genuine output of that routine.

Neither this routine nor its dispatched reviewer could rule out everything:
nothing cryptographically ties the commit to that session, and the
reviewer's own transcript was not read to confirm it did the work its notes
describe. The real, already-documented weakness this surfaces is that
BuildCraft's `reviews/<pr>/` gate rests entirely on self-attested
`reviewer_id`/`commit_author_id`/`reviewer_was_dispatched_by_author` fields
under one shared mandated commit identity (BuildCraft's own
`reviews/README.md` KNOWN WEAKNESS section) — not a new finding, but the
honest reason this routine's initial suspicion could not be fully resolved
either way. This is not a reason to withhold MERGE on #58's own deliverable.

## Not checked

- The real `LWB_PRIVATE_NEEDLES` scan (used a placeholder; only know CI's
  own step reported success).
- CI job step-level logs (read check-run conclusions only, via the API — raw
  log blobs are proxy-blocked).
- `lwb_check_proof.py --reexecute` digests.
- Anything beyond Linux, `dontAsk` permission mode, one event per matcher —
  deny-mode policy behavior is untested, as the doc itself discloses.
- A full independent re-review of #57's content beyond the deliverable diff
  and the history-leak scan (covered in #57's own review record).
- The reviewer session's full transcript and anything tying its commits to
  that session beyond session metadata, timing and branch name.

## JSON record (BuildCraft reviews/README.md shape — reference only; this
routine is read-only on LEAPWare-BuildCraft and did not post this record
there)

```json
{
  "pr": 58,
  "reviewed_commit": "2e27b0889f5cf42d0975db56d5f6edc197673b3b",
  "reviewer_agent": "claude",
  "reviewer_id": "shellux-cto-cloud-reviewer-2026-09-19",
  "commit_author_agent": "claude",
  "commit_author_id": "claude-code-track-a-session-012SWAwJYJ1cUjtpPZFhHZK2-2026-09-24",
  "verdict": "AGREE",
  "reviewer_was_dispatched_by_author": false,
  "notes": "MERGE: re-cut of #57 (DO NOT MERGE, reviewed separately) that fixes the permanent env-leak-history failure by never introducing the leak in this branch's own history (verified: lwb_check_env_leak.py --range clean on this branch, failing on #57's). The doc's core claim (hooks.json firing for Bash and Agent matchers, caused specifically by hooks.json per a negative control) is independently reproduced from scratch. 592 tests pass (591+1 in this reviewer's shallow clone, the 1 failure being a shallow-clone artifact reproduced identically at base; a separate unshallowed clone in the same run reported a clean 592). Investigated the tip commit reviews/58/cloud-reviewer-b.json as a possible planted fake independent review; session metadata (get_session) indicates it is a genuine separate scheduled reviewer session distinct from the author's, and its paired record on #57 is a DISAGREE -- inconsistent with gaming this gate. Not confirmed as fabricated, though full independence is not provable from this repo alone; see BuildCraft's own reviews/README.md KNOWN WEAKNESS section. commit_author_id above is read from the branch's own review-record content (session token found in-repo), not independently verified by this routine."
}
```
