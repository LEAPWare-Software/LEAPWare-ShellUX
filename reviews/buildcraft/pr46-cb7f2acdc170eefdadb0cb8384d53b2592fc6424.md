Verdict: DISAGREE

Reviewed by: shellux-cto-cloud-reviewer-2026-09-19 (dispatched lw-verifier, opus)
Reviewed PR: LEAPWare-Software/LEAPWare-BuildCraft#46
Reviewed head: cb7f2acdc170eefdadb0cb8384d53b2592fc6424

Summary: the deliverable content (D1/D2, and the "dropped content already exists
on main" claim) checks out. But the re-cut's own stated rationale for existing —
"a stale record can never be cleared, only re-cut" — is true of #46 itself at
this exact head: the gate is currently failing (exit 1) on a stale record, the
same defect class this PR was created to escape. Several verification numbers in
the PR body are stale/false at this head, and one commit silently replaced
another reviewer's record and identity in place.

## Findings, most serious first

**F1 (blocking): the stale-record veto this PR exists to escape is live on #46's own head.**

```
$ python3 scripts/lwb_lanes.py --base 6afc545646faa9b241ffe60ee4c612f50e4437f8 --head cb7f2acdc170eefdadb0cb8384d53b2592fc6424 --pr-number 46
EXIT=1
FAIL: reviews/46/cloud-reviewer.json: STALE — this record reviewed 'fd2a6fbcd22ac5fa8302fbe9b469965372bfc1ba', but the current REVIEWABLE head is '01221a756ffdb5a681ccf38cec1261c9196c41d2'
reviewable head 01221a756ffdb5a681ccf38cec1261c9196c41d2
```
Two valid AGREE records already exist at the correct reviewable head from
distinct reviewer identities (only 1 is required), but `independent_reviews()`
passes one shared `errors` list into every `_review_ok()` call, so the one
stale leftover record still vetoes the whole PR. The PR's own `HANDOFF.md`
(lines 32-35, at this head) already documents it: "stale by design (D20),
needs a fresh independent review." By this PR's own stated rationale for why
#43 had to be abandoned, #46 now needs a re-cut too. (PR #54, reviewed
separately in this same run, proposes exactly this fix to `independent_reviews`
but has not merged.)

**F2 (confirmed): claim 1's stated cause is imprecise; the DISAGREE count is short.**
`_review_ok`'s verdict check does run before its staleness check, but that
ordering isn't what matters — F1 shows a stale *AGREE* triggers the identical
hard failure. The real cause is the shared `errors` list in
`independent_reviews`. Separately, the old `reviews/43/` branch held **three**
DISAGREE records (`01TkEV63` at 7da6f47, `01Bp84dN` at 220c121, `01SK1GEB` at
a31f0ef), not the two the PR body names.

**F3 (confirmed): claim 4 ("zero records under reviews/46/") is false at head.**
```
$ git ls-tree -r cb7f2ac -- reviews/46/
reviews/46/cloud-reviewer-b.json
reviews/46/cloud-reviewer-clock-016Yb3Nh.json
reviews/46/cloud-reviewer.json
```
None of the three was committed by the PR author's own session (01EkvcWA) —
committing sessions are 013c324w/01LbcFTH and 016Yb3Nh — so this is not a D20
self-review violation, just a stale sentence in the PR body.

**F4 (confirmed): one reviewer identity overwrote another's record in place, which
the self-attested-identity weakness in `reviews/README.md` cannot distinguish
from a reviewer refreshing its own record.**
```
1862e2c  A reviews/46/cloud-reviewer-b.json   reviewer-cloud-routine-b-buildcraft-013c324wcGen4ZcmbPpvikNL  @ fd2a6fb
cb7f2ac  M reviews/46/cloud-reviewer-b.json   reviewer-cloud-routine-b-buildcraft-01LbcFTHtPA9EX4QfFvsS5Nh  @ 01221a7
```
`cb7f2ac` (session 01LbcFTH) replaced session 013c324w's record and identity,
labelled "refresh". Separately, `bdf8b86` added `cloud-reviewer.json` naming
session `01CSRfez` with **no** `Claude-Session` trailer on that commit — nothing
ties that identity to whoever actually pushed it.

**F5 (confirmed, defect shape: a field read while its meaning-changing sibling is
ignored): the records disagree with each other about who authored the content.**
Content commits (4191446, ba983cf, 804a153, fd2a6fb, 01221a7) all carry
`Claude-Session: session_01EkvcWAEAV2sJYoN51CPrTF`. `cloud-reviewer-b.json`'s
`commit_author_id` matches that. But `cloud-reviewer.json` and
`cloud-reviewer-clock-016Yb3Nh.json` instead use
`coordinator-opus5-f8da3f9e-2026-09-19` (copied from `proof/46.json`'s stale
`#32`-era `author` field). `_review_ok`'s self-review guard compares
`reviewer_id` only against whatever `commit_author_id` a record declares about
itself — for two of the three records that comparison ran against the wrong
identity.

**F6 (confirmed): D1's fix swaps one transient claim for another that is already
false.** `reviews/27/dispatch-correction.md` at head (lines 114-121) now reads
"...So this PR is not currently a live instance: its green `lwb-lanes` does
attest to review of that commit's content." `lwb-lanes` is NOT green at this
head (F1, exit 1) — the sha is gone from the sentence, but the sentence still
makes a point-in-time CI-status claim, and it's false right now. The same
paragraph still says the carrying re-cut was "PR #41."

**F7 (confirmed, false claim): the `proof/39.json` mismatch is not confined to
this PR.**
```
$ python3 scripts/lwb_check_proof.py --reexecute   # at main 6afc545
REEXIT=1
FAIL: proof/39.json: python3 scripts/lwb_handoff.py --check: digest mismatch — recorded sha256 'ab8f5e3b…', re-run sha256 'b9faccbc…'
```
The PR body claims this mismatch "does not occur against unmodified main" —
it does. `HANDOFF.md` is 2942 bytes on main and 2993 at the PR head; any edit
breaks `proof/39.json`'s recorded digest. This is `verifiable: true` on a
digest that can't survive a later HANDOFF.md edit — CI runs it report-only
(`ci.yml:130`), so nothing blocks on it today, but the claim is false.

**F8 (confirmed): verification numbers are stale.** pytest at head: `592 passed`
(not 522). `lwb_check_proof.py --pr 46`: `23 record(s)` (not 22). `proof/46.json`
itself records `512 passed` and `19 record(s)` — an older snapshot still. These
commands are `verifiable: false`, so nothing catches the drift mechanically.

## Held up

- D2: `git log -1 --format=%H -- reviews/27/dispatch-correction.md` = `ba983cf686ea…`, matching `proof/46.json`'s `commit`. No later commit touches the file.
- "Dropped content already exists on main": blob ids match between the old `pr/43` branch and `main` for `docs/maintainers/session-handoff-2026-09-19.md`, `docs/requirements/build-plan.md`, `docs/requirements/decisions.md`, `proof/39.json`; `git diff --stat` for `reviews/{30,35,38,39}` is empty.

## Not checked

- Each of the "9 kept commits" against its `pr/43` original individually (only `dispatch-correction.md` diffed in full).
- Whether the dropped `proof/43.json` regeneration commit held anything needed.
- Checker scripts beyond `lwb_check_proof` (`--pr 46`, `--reexecute`) and `lwb_handoff --check`.
- GitHub CI run status/timestamps for #46, or who fired the three reviewer routines whose records collide in F4/F5.
- Whether `lwb_check_state_claims` being `verifiable: false` is itself a shape-(c) exemption on deterministic output — plausible, not confirmed.
- #54 beyond its `independent_reviews` change (reviewed separately in this run).

```json
{
  "pr": 46,
  "reviewed_commit": "cb7f2acdc170eefdadb0cb8384d53b2592fc6424",
  "reviewer_agent": "claude",
  "reviewer_id": "shellux-cto-cloud-reviewer-2026-09-19",
  "commit_author_agent": "claude",
  "commit_author_id": "session_01LbcFTHtPA9EX4QfFvsS5Nh",
  "verdict": "DISAGREE",
  "reviewer_was_dispatched_by_author": false,
  "notes": "F1: lwb_lanes.py --pr-number 46 exits 1 at this exact head on a STALE record veto -- the same defect class the PR exists to escape, with a fix pending in #54. F3/F7/F8: several verification claims (records count, pytest count, proof/39 mismatch scope) are false or stale at this head. F4/F5: one reviewer identity overwrote another's record in place, and two of three review records checked self-review against the wrong commit_author_id."
}
```
