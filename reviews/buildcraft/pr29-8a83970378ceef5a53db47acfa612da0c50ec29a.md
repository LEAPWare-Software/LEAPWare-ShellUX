Verdict: DISAGREE

**Scope note before the findings.** The dispatch brief described this PR as 52 files touching `core/lwb_core/{config,engine,events}.py`, `rules/lwb_proof_required.py`, `scripts/lwb_check_hook_launch.py`, `adapters/claude/{hook_io,repo_facts}.py`, `plugins/*/vendor/`, with five deleted test files and a workflow rename. **None of that is in this PR.** Measured:

```
$ git diff --name-status -M 9b374b52a56e04e6b443f9aa9d86fa51a0d8a47c HEAD
A  .claude/agents/lw-{architect,explorer,implementer,scribe,verifier}.md
A  .github/workflows/lwb-auto-queue.yml
M  HANDOFF.md
A  docs/cloud/{ledger-issue.md,runbook.md,routines/{README,conductor,reviewer,watchdog}.md}
A  proof/29.json
A  reviews/29/cloud-reviewer.json, reviews/29/cloud-reviewer-2.json
A  scripts/lwb_auto_queue.py
M  scripts/lwb_lanes.py
A  tests/test_lwb_auto_queue.py
M  tests/test_lwb_lanes.py
20 files changed, 2686 insertions(+), 30 deletions(-)
```
Zero deletions of any file, zero renames, no `core/` change, no `scripts/lwb_check_foreign_repo.py` anywhere in the repo at either ref. The brief's file list is in fact the shape of this branch diffed against **live** `main`, not against its base — which is itself finding 1. Items 4 (vendor trap) and 5 (deleted tests) of the brief therefore had nothing to bite on here; I ran the vendor check anyway and it is clean.

---

### 1. BLOCKING — the deliverable is already on `main` by another route, and merging this adds a second, divergent, known-broken copy of it. CONFIRMED.

`origin/main` has advanced three merges past this PR's base (`9b374b5`) and already carries the merge path:

```
$ git log --oneline -3 origin/main
bfe8507 The built-in token enqueues and then stalls, silently (#34)
de27f72 Re-cut the cloud merge path onto a branch the lane gate can pass (supersedes #28) (#31)
f9b4dea Prove the plugin works in a repo that is not this one (#27)

$ git ls-tree -r --name-only origin/main | grep -E "auto.queue|auto_queue"
.github/workflows/auto-queue.yml
scripts/cloud/auto_queue.py
tests/test_auto_queue.py
```

Git sees no path collision, so the merge silently keeps both implementations:

```
$ git merge-tree --write-tree --name-only origin/main HEAD
e24ea85aec0eb5140e58639e4f7751fcc6c7bfb7
HANDOFF.md
Auto-merging HANDOFF.md
CONFLICT (content): Merge conflict in HANDOFF.md

$ git ls-tree -r --name-only e24ea85 | grep -E "auto.queue|auto_queue"
.github/workflows/auto-queue.yml
.github/workflows/lwb-auto-queue.yml
scripts/cloud/auto_queue.py
scripts/lwb_auto_queue.py
tests/test_auto_queue.py
tests/test_lwb_auto_queue.py
```

Two auto-queue workflows, two merge-decision scripts, two test files, one HANDOFF.md conflict (GitHub reports `mergeable_state: "dirty"`).

Worse than duplication: `main`'s commit `bfe8507` (#34) exists **because** the built-in token is the wrong token. Its message: *"an enqueue made with the built-in GITHUB_TOKEN creates the queue entry, but GitHub does not start workflow runs for events triggered by that token — so the merge_group checks never run and the pull request sits in the queue forever, green… `${{ secrets.AUTO_QUEUE_TOKEN || github.token }}` would be the precise defect this repository keeps shipping."* `main`'s workflow now hard-fails when `AUTO_TOKEN_QUEUE` is absent:

```
$ git show origin/main:.github/workflows/auto-queue.yml | grep -n -i token
 60:          GH_TOKEN: ${{ github.token }}          # decide step only
 89:      - name: Require the AUTO_TOKEN_QUEUE secret
 94:          if [ -z "$AUTO_TOKEN_QUEUE" ]; then
109:          GH_TOKEN: ${{ secrets.AUTO_TOKEN_QUEUE }}   # the enqueue step
```

PR #29's workflow, at `.github/workflows/lwb-auto-queue.yml` lines 79–84, wires the **enqueue-capable** step to `GH_TOKEN: ${{ github.token }}` with no secret and no loud failure. Arming `LWB_AUTO_QUEUE` — which the PR body presents as one of two owner actions to turn it on — therefore reproduces exactly the stall #34 was merged to prevent, in the one job that merges code with nobody watching. Nothing in this PR references `AUTO_TOKEN_QUEUE`; the branch predates the discovery and was never rebased onto it.

### 2. BLOCKING — the new `review_records()` gate is strictly weaker than the one it duplicates: a review record with **no** `reviewed_commit` at all satisfies it. CONFIRMED.

`scripts/lwb_auto_queue.py:197,206`:
```python
reviewed = str(record.get("reviewed_commit", ""))
...
if not head.startswith(reviewed):
```
`"anything".startswith("")` is `True`.

```
control: reviewed_commit == full head            ready=True  reasons=[]
control: reviewed_commit == a different sha      ready=False reasons=['reviews/7/cloud.json: reviewed_commit bbbbbbbbbbbb is not this head', ...]
MUTATION: reviewed_commit == "" (empty string)   ready=True  reasons=[]
MUTATION: reviewed_commit key ABSENT             ready=True  reasons=[]
MUTATION: reviewed_commit == "a" (1 char)        ready=True  reasons=[]
```

The repo already paid for this trap. `scripts/lwb_lanes.py:669-673` carries the hardened version *with a comment explaining the crash it fixed*:
```python
if (
    not isinstance(reviewed_commit, str)
    or len(reviewed_commit) < 7
    or not head_sha.startswith(reviewed_commit)
):
```
The new file has neither the `isinstance` nor the `len >= 7` guard. Five further divergences, all reproduced:

```
control                                                        ready=True
MUTATION: commit_author_id ABSENT                              ready=True
MUTATION: reviewer_was_dispatched_by_author = TRUE             ready=True
MUTATION: record's own 'pr' field = 9999 (wrong PR)            ready=True
MUTATION: reviewer/author share 'lw-architect-opus5-af50c414'  ready=True
```

`lwb_lanes` rejects or flags every one of those: missing `commit_author_id` (line 651), non-boolean/absent `reviewer_was_dispatched_by_author` (line 709), record `pr` mismatch (line 640), and an ≥8-char shared identity segment (`_shared_long_segment`, line 699 — *"likely the same session or dispatch reviewing its own work"*). The `reviewer_was_dispatched_by_author` case is the requested defect shape exactly: a sibling field that inverts the record's meaning, read by one implementation and ignored by the other.

This is not defence-in-depth. `scripts/lwb_auto_queue.py:69-73` states the whole design reason for the duplication — *"this script must run against a PR's records while the workflow has the DEFAULT branch checked out"* — i.e. it is the trusted-code-reading-untrusted-records path, the backstop for a PR that edits `lwb_lanes.py` itself. Its justification comment says *"a mismatch is caught by tests/test_lwb_auto_queue.py"*. The only such test, `test_required_review_count_matches_the_lane_gate` (line 354), compares **one integer** (`REQUIRED_INDEPENDENT_REVIEWS`). Nothing compares the semantics.

### 3. BLOCKING — the PR's own proof record, which says in the field that nobody checked it, passes the new proof gate. CONFIRMED.

`proof/29.json` declares `"checked_by": "UNCHECKED-pending-independent-review"`. `scripts/lwb_auto_queue.py:246` tests only `proof.get("checked_by") == proof.get("author")`.

```
REAL proof/29.json (checked_by=UNCHECKED-...)  ready=True
control: checked_by == author                  ready=False
```
A literal string announcing that no one checked the record satisfies "an independent party checked this record".

### 4. BLOCKING — the workflow-safety guard tests that `ref` exists, not what it contains. CONFIRMED.

Control (the guard is not wholly vacuous): adding a `pull_request:` trigger with the checkout left unpinned does fail it —
```
$ python3 -m pytest tests/test_lwb_auto_queue.py -q
tests/test_lwb_auto_queue.py:412: AssertionError
FAILED ...::test_the_workflow_never_checks_out_pull_request_code_under_a_write_token
1 failed, 26 passed in 0.05s
```
But `tests/test_lwb_auto_queue.py:411` is `unpinned = [s for s in steps if not (s.get("with") or {}).get("ref")]` — truthiness only. Introducing the canonical GitHub Actions privilege-escalation shape into the real workflow (`pull_request_target:` + `ref: ${{ github.event.pull_request.head.sha }}`, under `permissions: pull-requests: write`):
```
$ python3 -m pytest tests/test_lwb_auto_queue.py -q
27 passed in 0.04s
```
The assertion's own message (line 415) names the correct value — *"Pin `ref: ${{ github.event.repository.default_branch }}`"* — and the predicate does not enforce it. Fork-PR-controlled code under a write token, guard green. This is the previous reviewer's finding A; I reproduced it independently and with the strictly worse `pull_request_target` trigger.

### 5. BLOCKING — the meta-test written to prove that guard non-vacuous does not call the guard. CONFIRMED.

`test_that_guard_actually_fails_when_the_hazard_is_introduced` (line 420) re-implements the predicate over literal dicts (lines 432–434) instead of invoking the real one. Blanking the real predicate **and** leaving the real hazard in the real workflow:
```
$ (workflow gains `pull_request_target:`; test line 411 replaced by `unpinned = []`)
$ python3 -m pytest tests/test_lwb_auto_queue.py -q
27 passed in 0.06s
```
Commit `41c1690`'s message claims the companion test means *"the guard cannot quietly become vacuous again without that test failing too"*. That claim is false. Previous reviewer's finding B, reproduced.

### 6. BLOCKING — the five new agent files put an HTML comment ahead of the YAML frontmatter, unlike the reference they were adapted from. CONFIRMED divergent / PLAUSIBLE inert.

Every `.claude/agents/lw-*.md` in this PR begins with a 10-line `<!-- ... -->` block; the `---` frontmatter opens at line 12. The PR states it adapted these from LEAPWare-ShellUX's `.claude/agents/`, where the order is reversed:
```
$ head -8 /home/user/LEAPWare-ShellUX/.claude/agents/lw-architect.md
---
name: lw-architect
description: ...
model: opus
effort: high
---

<!--
```
All six ShellUX role files open with `---` on line 1. Subagent frontmatter is parsed only when the file starts with the delimiter, so these five files very likely register no agent at all — which makes the PR's stated deliverable ("five roles, committed so a routine can see them") inert while looking committed. Nothing in the repo validates them:
```
$ grep -rn "claude/agents" --include=*.py scripts/ tests/ core/
scripts/lwb_lanes.py:77   (comment)
scripts/lwb_lanes.py:86   (comment)
tests/test_lwb_lanes.py:50,53,58   (path-classification assertion only)
```
The only test naming the path asserts `classify_path(".claude/agents/lw-verifier.md") == "shared"`. The PR's own `runbook` unproven register lists *"that `.claude/agents/` is visible to a routine"* — so the file shape was never exercised, and it differs from the one shape known to work. I could not execute the host loader here, hence PLAUSIBLE rather than CONFIRMED on inertness; the divergence from the reference is CONFIRMED by the command above.

### 7. BLOCKING — a required check is red at this head, and no required check has reported at this head at all. CONFIRMED.

```
$ python3 scripts/lwb_check_state_claims.py --repo .
HANDOFF.md:36: UNVERIFIABLE: timestamp field, not checked: Generated: 2026-09-19 14:39 UTC
HANDOFF.md:41: UNVERIFIABLE: Open PRs listing could not be fully re-derived (no gh auth here)
FAIL: HANDOFF.md:37: [stale main SHA in generated block] main SHA: f9b4deadbb441f0c1046a0d73b0434424f1cf13c (live: bfe850796969980545811e1ffa551b9143965faf, live main's first parent: 'de27f7230da32ccdeb25374781f81d60ad5e9940')
EXIT=1
```
`proof/29.json` records this exact command as `"exit": 0, "expect_exit": 0`. It was true at `41c1690`; `main` has since moved two merges further and it is no longer true. The command is a CI step (`.github/workflows/ci.yml:184`), inside the `test` job that reports the required contexts.

GitHub state at the reviewed head:
```
pull_request_read get_check_runs  -> total_count: 1  [{"name":"decide","conclusion":"success"}]
pull_request_read get_status      -> {"state":"pending","total_count":0,"statuses":[]}
actions_list ci.yml branch=cloud/runbook -> total_count: 2, both "conclusion":"failure"
   at 8d75890ba213... and 74d0590a0f15...
```
The single check run at head is the PR's own push-triggered `decide` job. CI has not run on the last four commits — consistent with the HANDOFF.md merge conflict that arose when `main` advanced at ~14:18Z, right after the last CI run at 14:14Z. The PR cannot go green in its present state for reasons independent of any verdict.

### 8. BLOCKING (replicated, not merely inherited) — the superseded-DISAGREE deadlock, reproduced exactly.

`scripts/lwb_lanes.py:646` checks `verdict` **before** the freshness check at line 670, so an adverse record pinned to an older commit fails forever. Current state:
```
$ python3 scripts/lwb_lanes.py --base 9b374b52... --head HEAD --pr 29
FAIL: reviews/29/cloud-reviewer-2.json: verdict is 'DISAGREE', want 'AGREE'
FAIL: reviews/29/cloud-reviewer.json: verdict is 'DISAGREE', want 'AGREE'
... (22 FAIL lines)   EXIT=1
```
Adding a fresh, well-formed `AGREE` record at the actual reviewable head (`resolve_reviewable_head(...)` → `ae29b1a98ae935c79889c04399d7fbcfb89bf52c`) does not help — still 22 FAIL lines, EXIT=1. Removing the two adverse records is the only satisfying move:
```
$ (probe-agree.json added AND cloud-reviewer{,-2}.json removed)
$ python3 scripts/lwb_lanes.py --base 9b374b52... --head HEAD --pr 29
lwb-lanes check passed
EXIT=0
```
(Clone restored afterwards; `git status --porcelain` empty at `8a83970`. Nothing was pushed.)

The deadlock in `lwb_lanes.py` is pre-existing on `main`. What is chargeable here is that `readiness()` in the **new** `scripts/lwb_auto_queue.py` reproduces the identical ordering — line 198 rejects the verdict before line 206 tests freshness — and every rejection lands in `reasons`, which gates readiness at line 257:
```
SUPERSEDED DISAGREE at an OLD head + fresh AGREE at head       ready=False
      - reviews/7/stale.json: verdict is 'DISAGREE', not AGREE
```
So the known defect is being copied into the one code path that merges without a human present.

### 9. Non-blocking, but the gate change ships without its compensating check.

`SHARED_PREFIXES` gains `.claude/` and `.codex/`. `check_lanes` treats `other` as a hard error (*"outside the {agent} lane and not a shared path"*), so this is a pure widening: those trees went from "no agent may commit them" to "either agent may commit them with one review record". `.claude/settings.json` and `.codex/hooks.json` already exist on `main`, and `.claude/settings.json` is what registers the PreToolUse lane hook. Nothing in CI verifies it still does:
```
$ head -6 scripts/lwb_check_hook_launch.py
"""CI check backing the `lwb-portable` job: run each plugin's hook EXACTLY
as declared ... For each of `plugins/claude/lwb/hooks/hooks.json` and
`plugins/codex/lwb/hooks/hooks.json` ..."""
```
It reads the plugin manifests, never `.claude/settings.json`. The PR body's claim that the change *"widens what needs an independent review record"* is not accurate in the direction implied — before it, those paths needed no record because they were unreachable.

### 10. Non-blocking — `required_contexts()` reads `context` and ignores every sibling that changes its meaning. CONFIRMED.

`scripts/lwb_auto_queue.py:142-157` iterates all rulesets and reads only `c["context"]`, ignoring the ruleset's `enforcement`, `target` and `conditions`, and the rule's `integration_id`:
```
required_contexts() from a DISABLED ruleset scoped to release/*: ['a-check-that-never-runs-on-main']
integration_id in the rule: 99999
integration_id read by the script: NO
```
Fail-closed (it over-collects, so it refuses rather than enqueues), which is why this is non-blocking — but ignoring `integration_id` means any app may satisfy a required context by name, and a ruleset in `evaluate`/`disabled` mode contributes contexts that will never report.

Related and also fail-closed: `check_conclusions()` reads only `/commits/{sha}/check-runs`. A required context satisfied by the **commit statuses** API (`POST /repos/.../statuses/{sha}`) would read as *"has not reported"* forever.

### 11. Non-blocking — the role file's declared model contradicts the routine that dispatches it.

`.claude/agents/lw-verifier.md` frontmatter pins `model: opus`. `docs/cloud/routines/reviewer.md:8` and `:99-100` say *"Choose `opus` when the pull request touches `core/`, `scripts/lwb_*`, `.github/` or any rule; `sonnet` otherwise."* The agent file is the only mechanism that sets the model for a named subagent, and it admits no per-dispatch override, so the `sonnet` half of the rule cannot be honoured. (The ShellUX reference file declares `model: sonnet` for the same role.) The other four roles are internally consistent.

### 12. Non-blocking — count drift in the evidence.

`proof/29.json`'s `mutations` entry and `acceptance_criteria` both say *"26 tests"* / *"26 refusal-path tests"*; the file collects 27:
```
$ python3 -m pytest tests/test_lwb_auto_queue.py --collect-only -q | tail -1
27 tests collected in 0.03s
```

### What is genuinely sound, stated plainly

- **Full suite is green.** `python3 -m pytest tests/ -q` → `515 passed, 1 warning in 18.33s`, `PYTEST_EXIT=0`. One pre-existing `DeprecationWarning` at `tests/test_lwb_check_proof_reexecute.py:248`, not introduced here.
- **Vendor trap: clean.** `python3 scripts/lwb_build.py --check` → `OK: .../plugins/claude/lwb/vendor matches source`, `OK: .../plugins/codex/lwb/vendor matches source`, exit 0. Independently: `diff -r -x '__pycache__' core/lwb_core plugins/{claude,codex}/lwb/vendor/lwb_core` → both `IDENTICAL`. (This PR changes no `core/` file, so there was nothing to mirror.)
- **`python3 scripts/lwb_check_proof.py`** → `lwb-proof check passed (19 record(s))`, exit 0.
- **The `verifiable: false` exemption on the pytest command is correctly applied.** `verifiable_reason: "nondeterministic-output"` is in `ALLOWED_VERIFIABLE_REASONS`, and the sanitiser genuinely does not normalise the duration: `lwb_sanitise.sanitise('515 passed in 47.33s')` → `'515 passed in 47.33s'`. No misapplied determinism claim found.
- **The other five commands in `proof/29.json` carry `verifiable: true`**, and four of the five reproduce at this head. Only `lwb_check_state_claims.py` no longer does (finding 7).
- The previous reviewer's two *blocking* findings (state-claims red; the substring-search guard) are genuinely closed as literal cases — I reproduced the state-claims fix intent and the guard's control failure. Findings 4 and 5 are the residue inside those fixes.
- Section 4's stated rule (`.claude/`/`.codex/` shared) **is** covered by a test that actually asserts the behaviour: `test_classify_cli_config_dirs_are_shared`, three assertions, passes.

Not checked:
- **Whether the Claude Code subagent loader actually rejects frontmatter preceded by an HTML comment.** I compared against the working reference implementation and found the divergence, but I did not execute the host's parser, so finding 6's inertness is PLAUSIBLE, not CONFIRMED. Anyone with a Claude Code session can settle it in one command.
- **The live ruleset contents.** I could not call `/repos/.../rulesets`, so the PR's "nine required checks, not the six documented" claim (`docs/cloud/runbook.md:125`) is unverified by me; I took the check-run and combined-status readings from the API instead.
- **The `enqueuePullRequest` mutation.** Never executed — same hole the PR declares. I also did not run `scripts/lwb_auto_queue.py --probe` against the live API (no `gh` auth in this container); all auto-queue behaviour above was exercised through monkeypatched read helpers.
- **Whether `main`'s `scripts/cloud/auto_queue.py` has the same weaknesses as findings 2 and 3.** I read only its workflow's token wiring. If it shares them, they are pre-existing on `main` and belong in their own PR; that does not reduce finding 2, because #29 adds a *second* copy.
- **`docs/cloud/runbook.md` (401 lines), `ledger-issue.md`, and the three routine specs** were read only for the specific consistency checks reported (models, TRACK-vs-lane framing, required-check counts, the unproven register). I did not audit every prose claim in them against a test.
- **The cloud-proxy premise of the whole design** — that GraphQL is blocked from a routine — is ShellUX's measurement carried on trust. The PR says so in its own unproven register; I did not attempt to measure it.
- **Windows and macOS behaviour.** Everything above ran on Linux/Python 3.11.15; CI matrices 3.10/3.12 across three OSes were read from logs, not re-run.
- **`reviews/README.md`** — the brief asked me to read it for schema and norms; no such file exists in the repo at this head (`reviews/schema.json` is what `docs/cloud/routines/reviewer.md:107` points records at). I validated record shape against `lwb_lanes._review_ok` and `lwb_check_proof.py` instead.
- I did not attempt to determine *why* GitHub stopped scheduling CI after 14:16Z. The merge conflict is the obvious candidate and the timing fits, but I did not confirm the mechanism.

```json
{
  "pr": 29,
  "reviewed_commit": "8a83970378ceef5a53db47acfa612da0c50ec29a",
  "reviewer_agent": "claude",
  "reviewer_id": "shellux-cto-cloud-reviewer-2026-09-19",
  "commit_author_agent": "claude",
  "commit_author_id": "lw-architect-opus5-af50c414-2026-09-19",
  "verdict": "DISAGREE",
  "reviewer_was_dispatched_by_author": false,
  "notes": "Eight blocking findings, all reproduced in an isolated clone; nothing pushed, tree verified clean at 8a83970 afterwards. (1) SUPERSEDED: origin/main advanced three merges past this PR's base and already carries scripts/cloud/auto_queue.py, .github/workflows/auto-queue.yml and tests/test_auto_queue.py via #31/#34; git merge-tree --write-tree origin/main HEAD yields a tree holding BOTH implementations plus a HANDOFF.md conflict (mergeable_state dirty). main's #34 exists because the built-in GITHUB_TOKEN enqueues then stalls silently and now requires AUTO_TOKEN_QUEUE with no fallback; #29's workflow wires the enqueue step to GH_TOKEN: github.token, so arming it reproduces the exact defect #34 removed. (2) The new scripts/lwb_auto_queue.py review_records() re-implements the review gate strictly weaker than lwb_lanes._review_ok: str(record.get('reviewed_commit','')) + head.startswith(reviewed) means an ABSENT, EMPTY or 1-CHAR reviewed_commit passes; a full different sha correctly fails. It also ignores reviewer_was_dispatched_by_author (=true passes), the record's own pr field (=9999 passes), missing commit_author_id (passes) and the >=8-char shared-identity self-review heuristic (passes) -- all of which lwb_lanes rejects. The duplication's justifying comment cites tests/test_lwb_auto_queue.py; the only comparison test compares one integer. This is the trusted-code-vs-PR-records backstop, so the weakness is not defence-in-depth. (3) proof/29.json declares checked_by 'UNCHECKED-pending-independent-review' and satisfies the new proof gate, which tests only checked_by == author. (4) The workflow guard tests that with.ref is non-empty, not what it says: pull_request_target + ref: github.event.pull_request.head.sha under pull-requests: write leaves 27/27 passing; the control (unpinned + pull_request) does fail, 1 failed/26 passed. (5) test_that_guard_actually_fails_when_the_hazard_is_introduced re-implements the predicate over literal dicts; blanking the real predicate WHILE the real workflow carries the real hazard leaves 27/27 passing, so 41c1690's claim that the guard cannot silently go vacuous is false. (6) All five .claude/agents/lw-*.md open with a 10-line HTML comment before the --- frontmatter; all six ShellUX reference files they were adapted from open with --- on line 1 -- plausibly inert, nothing in the repo validates them, and the PR's own unproven register admits agent visibility to a routine was never tested. (7) python3 scripts/lwb_check_state_claims.py --repo . exits 1 at this head (stale main SHA f9b4dea vs live bfe8507) although proof/29.json records exit 0; it is a CI step at ci.yml:184. GitHub shows total_count 1 check run at head ('decide', the PR's own push job), combined status pending, and the only two CI runs on this branch both failed -- no required check has reported at this head. (8) The superseded-DISAGREE deadlock reproduced exactly: with both adverse records present, adding a fresh well-formed AGREE at the true reviewable head ae29b1a leaves lwb-lanes at 22 FAIL/exit 1; removing the two adverse records gives 'lwb-lanes check passed'/exit 0. Pre-existing in lwb_lanes, but readiness() in this PR's NEW file replicates the same verdict-before-freshness ordering. Non-blocking: .claude/ made shared widens .claude/settings.json (registers the PreToolUse lane hook) to either agent with one self-attested record while lwb_check_hook_launch.py validates only plugin hooks.json files; required_contexts() ignores ruleset enforcement/conditions and rule integration_id and reads only check-runs never commit statuses -- both fail-closed; lw-verifier.md pins model opus while reviewer.md instructs sonnet for non-gate PRs; proof/29.json says 26 tests where 27 collect. SOUND: 515 passed exit 0; lwb_build.py --check exit 0 and vendored copies IDENTICAL to core/lwb_core (PR touches no core/ file); lwb_check_proof.py passed 19 records; the verifiable:false/'nondeterministic-output' exemption on pytest is correctly applied. SCOPE: the dispatch brief's file list does not match this PR at all -- 20 files, all A/M, zero deletions, zero renames; that list is this branch diffed against LIVE main, which is finding 1."
}
```
